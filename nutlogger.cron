# Example cron job for nutlogger with flock
# Launches nutlogger if not running, checks every five minutes
*/5 * * * * flock -n /home/nutlogger/nutlogger.lock cd /home/nutlogger/nutlogger/ && /home/nutlogger/nutlogger/nutlogger
